import { escapePdfHtml as e, formatPdfBytes, humanizePdf, reportFamilyDocument } from './pdf-report-renderer.js';

const section = (title, body, intro = '', extra = '') => `<section class="section ${extra}"><h2>${e(title)}</h2>${intro ? `<p class="intro">${e(intro)}</p>` : ''}${body}</section>`;
const table = (headers, rows, widths = []) => `<div class="table-wrap"><table><colgroup>${headers.map((_, index) => `<col${widths[index] ? ` style="width:${widths[index]}"` : ''}>`).join('')}</colgroup><thead><tr>${headers.map((h) => `<th>${e(h)}</th>`).join('')}</tr></thead><tbody>${rows.map((row) => `<tr>${row.map((cell) => `<td>${cell}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;

export function lighthousePdfHtml(summary) {
  const o = summary.overview || {};
  const categories = o.categories || [];
  const label = { performance: 'Performance', accessibility: 'Accessibility', 'best-practices': 'Best Practices', seo: 'SEO' };
  const key = { performance: 'performance', accessibility: 'accessibility', 'best-practices': 'bestPractices', seo: 'seo' };
  const metrics = [['Pages analyzed', o.pages], ['Valid runs', o.validAudits], ['Failed runs', o.failedAudits], ['Important findings', o.totalFindings || 0], ...categories.map((id) => [label[id] || humanizePdf(id), o[key[id]] === '' ? 'Not assessed' : o[key[id]]])];
  const findings = (summary.insights?.categories || []).flatMap((category) => (category.groups || []).flatMap((group) => (group.findings || []).map((finding) => ({ category: category.title, group: group.title, ...finding })))).slice(0, 40);
  const findingCards = findings.length ? `<div class="grid">${findings.map((f) => `<article class="card"><span class="badge">${e(humanizePdf(f.status))}</span><h3>${e(f.title)}</h3><p>${e(f.category)} · ${e(f.group)} · ${f.affected?.length || f.occurrences || 0} affected result(s)</p><p>${e(f.displayValue || f.explanation || f.description || 'See the detailed Lighthouse HTML report.')}</p></article>`).join('')}</div>` : '<div class="card">No important Lighthouse findings were retained for the selected categories.</div>';
  const rows = (summary.rows || []).slice(0, 100).map((row) => [e(row.path), e(humanizePdf(row.device)), e(humanizePdf(row.status)), ...categories.map((id) => e(row[key[id]] === '' ? '—' : row[key[id]])), e(row.lcpMs === '' ? '—' : `${Math.round(row.lcpMs)} ms`), e(row.findingCount || 0)]);
  const sections = [section('Lighthouse score summary', `<div class="metrics">${categories.map((id) => `<div class="metric"><span>${e(label[id] || humanizePdf(id))}</span><strong>${e(o[key[id]] === '' ? 'Not assessed' : o[key[id]])}</strong></div>`).join('')}</div>`, 'Only categories selected for this run are shown.'), section('Important findings', findingCards, 'Grouped from Lighthouse category and audit evidence; no new severity model is introduced.'), section('Page results', table(['Page', 'Device', 'Status', ...categories.map((id) => label[id] || humanizePdf(id)), 'LCP', 'Findings'], rows, ['28mm', '14mm', '15mm']), `Up to 100 page/device rows are included; the HTML report and CSV retain the complete structured results.`), section('Methodology and limitations', '<div class="limitations">Scores and audits reflect the selected Lighthouse categories, devices, browser environment, run count, and tested URLs at generation time. Lighthouse scores are diagnostic measurements, not compliance or certification conclusions.</div>')];
  return reportFamilyDocument({ title: 'Lighthouse Reporter', projectName: o.projectName || 'Project', subtitle: 'Lighthouse performance and quality observations presented for technical remediation.', generatedAt: summary.generatedAt, metadata: [['Project', o.projectName], ['Base URL', o.baseUrl], ['Generated', summary.generatedAt], ['Browser mode', o.mode], ['Devices', (o.devices || []).join(', ')], ['Language', o.targetLanguage], ['Selected categories', categories.map((id) => label[id] || humanizePdf(id)).join(', ')]], metrics: metrics.slice(0, 4), sections });
}

export function assetPdfHtml(result) {
  const s = result.summary || {};
  const breakdown = Object.entries(s.breakdown || {}).sort((a, b) => b[1] - a[1]);
  const max = Math.max(...breakdown.map(([, value]) => value), 1);
  const breakdownHtml = `<div class="grid">${breakdown.map(([type, bytes]) => `<div class="card"><h3>${e(humanizePdf(type))}</h3><p>${e(formatPdfBytes(bytes))}</p><div class="bar"><i style="width:${Math.max(2, bytes / max * 100)}%"></i></div></div>`).join('')}</div>`;
  const pages = (result.pages || []).slice(0, 100).map((p) => [e(p.finalUrl), e(p.status), e(formatPdfBytes(p.totalTransferBytes)), e(p.requestCount), e(formatPdfBytes(p.breakdown?.script)), e(formatPdfBytes(p.breakdown?.image)), e(formatPdfBytes(p.thirdPartyBytes)), e(p.findings?.length || 0)]);
  const findings = (result.findings || []).slice(0, 40).map((f) => `<article class="card"><span class="badge">${e(humanizePdf(f.severity))}</span><h3>${e(f.title)}</h3><p>${e(f.detail)}</p><p><strong>Recommendation:</strong> ${e(f.recommendation)}</p><p>${e((f.pages || []).slice(0, 5).join(', '))}</p></article>`).join('') || '<div class="card">No configured page-weight thresholds were triggered.</div>';
  const assets = (result.largestAssets || []).slice(0, 40).map((a) => [e(humanizePdf(a.category)), e(a.url), e(formatPdfBytes(a.transferBytes)), e(a.host), e(a.pageUrl)]);
  return reportFamilyDocument({ title: 'Asset & Page-Weight Analyzer', projectName: result.projectName || 'Project', subtitle: 'Transferred-byte, request, resource, and optimization observations for the selected pages.', generatedAt: result.generatedAt, metadata: [['Project', result.projectName], ['Base URL', result.baseUrl], ['Generated', result.generatedAt], ['Browser', result.browser?.name], ['Viewport / device', result.device], ['Pages analyzed', s.pageCount]], metrics: [['Pages analyzed', s.pageCount], ['Average page weight', formatPdfBytes(s.averageBytes)], ['Average requests', Math.round(s.averageRequests || 0)], ['Third-party transfer', formatPdfBytes(s.thirdPartyBytes)]], sections: [section('Resource breakdown', breakdownHtml), section('Page results', table(['Page', 'Status', 'Weight', 'Requests', 'JavaScript', 'Images', 'Third-party', 'Findings'], pages, ['33mm']), 'Up to 100 page results are shown; CSV retains structured page data.'), section('Optimization findings', `<div class="grid">${findings}</div>`), section('Largest transferred assets', table(['Type', 'Asset', 'Size', 'Host', 'Source page'], assets, ['14mm', '52mm', '14mm', '28mm', '38mm']), 'The largest 40 retained assets are shown; the asset CSV remains the complete inventory.'), section('Methodology and limitations', '<div class="limitations">Transferred bytes and request counts reflect browser-observed network activity for the configured pages and viewport. Cache, compression, third-party behavior, and dynamic content can change between runs.</div>')] });
}

const ATTENTION = new Set(['broken', 'fragment_missing', 'server_error', 'unreachable', 'failed_to_check', 'client_error']);
const REVIEW = new Set(['redirected', 'restricted', 'rate_limited']);
export function brokenLinksPdfHtml(result) {
  const targets = result.targets || [];
  const attention = targets.filter((t) => ATTENTION.has(t.outcome));
  const review = targets.filter((t) => REVIEW.has(t.outcome));
  const healthy = targets.filter((t) => t.outcome === 'healthy');
  const outcomeRows = [...new Set(targets.map((t) => t.outcome))].sort().map((outcome) => [e(humanizePdf(outcome)), e(targets.filter((t) => t.outcome === outcome).length)]);
  const remediation = (list, limit = 100) => table(['Outcome', 'Status', 'Target', 'Type', 'Occurrences', 'Representative sources / reason'], list.slice(0, limit).map((t) => [e(humanizePdf(t.outcome)), e(t.httpStatus || '—'), e(t.targetUrl), e((t.referenceTypes || []).map(humanizePdf).join(', ') || t.referenceType), e(t.occurrenceCount || 0), e([...(t.sourcePages || []).slice(0, 3), t.failureReason || '', t.finalUrl && t.finalUrl !== t.targetUrl ? `Final: ${t.finalUrl}` : ''].filter(Boolean).join(' · '))]), ['17mm', '12mm', '52mm', '19mm', '14mm']);
  const types = Object.entries(healthy.reduce((acc, target) => { for (const type of target.referenceTypes || [target.referenceType || 'other_resource']) acc[type] = (acc[type] || 0) + 1; return acc; }, {})).sort((a, b) => b[1] - a[1]);
  return reportFamilyDocument({ title: 'Broken Links & Resources Checker', projectName: result.projectName || 'Project', subtitle: 'A remediation-first availability report preserving canonical outcome distinctions.', generatedAt: result.generatedAt, metadata: [['Project', result.projectName], ['Base URL', result.baseUrl], ['Generated', result.generatedAt], ['Scan scope', humanizePdf(result.scope?.mode)], ['Pages scanned', result.summary?.pagesScanned], ['Targets checked', result.summary?.uniqueTargets]], metrics: [['Pages scanned', result.summary?.pagesScanned], ['Targets checked', result.summary?.uniqueTargets], ['Needs attention', attention.length, 'attention'], ['Review', review.length, 'review'], ['Healthy', healthy.length, 'healthy']], sections: [section('Canonical outcome counts', table(['Outcome', 'Count'], outcomeRows, ['80mm'])), section('Attention required', remediation(attention), 'Definitive failures, missing fragments, server errors, and checks that could not complete are prioritized.'), section('Review items and redirects', remediation(review), 'Redirected, restricted, and rate-limited targets remain distinct from broken references.'), section('Healthy inventory summary', `${table(['Reference type', 'Healthy targets'], types.map(([type, count]) => [e(humanizePdf(type)), e(count)]), ['90mm'])}<p class="muted">${healthy.length} healthy targets are retained in the complete CSV; individual healthy rows are intentionally omitted from this PDF.</p>`), section('Scope, methodology, and limitations', '<div class="limitations">Only bounded HTTP(S) checks and rendered-page observations are represented. Restricted, rate-limited, redirected, timed-out, and failed checks are not classified as definitive broken links. Response bodies, credentials, headers, and sensitive query values are not included.</div>')] });
}
